interface:
  display_name: "Cron Loop"
  short_description: "Schedule recurring checks with optional Feishu alerts"
  default_prompt: "Use $cronloop to run a scoped health check every hour, optionally notify me in Feishu, and stop after verified completion."
